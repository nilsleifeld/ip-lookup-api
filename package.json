{
  "name": "ip-lookup-api",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/main.ts",
    "start": "NODE_ENV=production bun src/main.ts",
    "test": "bun test",
    "lookup-me": "bun scripts/lookup-me.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@maxmind/geoip2-node": "^6.3.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.2"
  }
}
